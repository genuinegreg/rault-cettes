version: "3"
services: 
  build:
    image: klakegg/hugo:latest
    volumes:
      - ".:/src"
  server:
    image: klakegg/hugo:latest
    command: server --baseURL "http://pi.hole"
    volumes:
      - ".:/src"
    ports:
      - "1313:1313"